'use strict';

/**
 * AI Service
 */

module.exports = () => ({
  // Service methods can be added here if needed for complex AI operations
  // For now, the controller handles all the logic directly
});
